# POLYGOTTEM SECURE SSH Callback Server Configuration
# TEMPEST Level C / Post-Quantum Cryptography
# Copy this file to .env and customize

# API Key for callback authentication (generate with: openssl rand -base64 32)
API_KEY=your-secure-api-key-here-change-this

# Secret key for Flask sessions (generate with: openssl rand -hex 32)
SECRET_KEY=your-secret-session-key-here-change-this

# Default admin password (CHANGE IMMEDIATELY after first login!)
# If not set, a random password will be generated and printed on startup
ADMIN_PASSWORD=CHANGE-THIS-PASSWORD-NOW

# Server port (internal, external port set in docker-compose.yml)
PORT=5000

# Database path (inside container)
DB_PATH=/data/ssh_callbacks_secure.db

# Session timeout in seconds (default: 3600 = 1 hour)
SESSION_TIMEOUT=3600

# Security Settings
# These are hardcoded for TEMPEST Level C compliance but documented here:
# - Hash Algorithm: SHA-512/384
# - Password Hashing: bcrypt (12 rounds)
# - Key Encapsulation: ML-KEM-1024 (Post-Quantum)
# - Digital Signatures: ML-DSA-87 (Post-Quantum)
# - TLS Version: TLSv1.3 only
# - Display Standard: TEMPEST Level C (Amber/Green on Black)
